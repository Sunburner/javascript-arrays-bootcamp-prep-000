var chocolateBars = [
  `snickers`,
  `hundred grand`,
  `kitkat`,
  `skittles`
]

function addElementToBeginningOfArray(array, element) {
  var check = [`foo`, 1]
  // chocolateBars[0] = `foo`
  // chocolateBars[1] = 1
  // return chocolateBars
  return check
}

  //const moreChocolate = [`hershey`, ...chocolateBars]


function destructivelyAddElementToBeginningOfArray() {

}
